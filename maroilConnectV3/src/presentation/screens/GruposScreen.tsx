import {FullScreenContruction} from '../components/iu/FullScreenContruction';
import {Layout} from '@ui-kitten/components';

export const GruposScreen = () => {
  return (
    <Layout style={{flex: 1}}>
      <FullScreenContruction message="Validando el flujo de los chat para desarrollar los grupos y poner funcionalidades adicionales como el envio de images." />
    </Layout>
  );
};
